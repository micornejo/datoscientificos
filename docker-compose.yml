version: "3.3"
services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bin/rails s -p 3000 -b '0.0.0.0' -e production"
    volumes:
      - "db:/datoscientificos/db"
    ports:
      - "3000:3000"
volumes:
  db:
